{% extends 'base.html.twig' %}

{% block title %}
    {{ test }}
{% endblock %}

{% block body %}
    {% if test%2 %}
        <h1>Titi</h1>
    {% else %}
        <h1>Tutu</h1>
    {% endif %}
    {% for t in tab %}
        <li>{{ t }}</li>
    {% endfor %}
    {% for d in tab_assoc %}
        <li>{{ d.id }} - {{ d.name }} - {{ d.value }}</li>
    {% endfor %}
    <a href="{{ path('accueilClass_toto') }}">lien vers toto</a>
{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block javacripts %}

{% endblock %}
